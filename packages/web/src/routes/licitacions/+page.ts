/**
 * `/licitacions` · `/es/licitacions` — PÀGINA EN CONSTRUCCIÓ.
 *
 * Decisió de direcció (Bea, 2026-06-18): per al llançament, el cabal de licitacions no publica
 * càlculs encara; la pàgina queda com a «en construcció» (enllaçada des del peu). La maquinària
 * es conserva per a la Fase 2 (`tools/export_licitacions.py`, l'artefacte, els contractes i els
 * helpers de format segueixen al repo, només desconnectats del consum a la UI).
 *
 * Pàgina estàtica: sense càrrega de dades, només prerender.
 */
export const prerender = true;
