# --- Secrets ---
.env
.env.*
*.key
*_secret*
.openrouter
# Excepció: defaults PÚBLICS del build del front (no és secret; vegeu packages/web/.env)
!packages/web/.env

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.uv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- Node / SvelteKit ---
node_modules/
.svelte-kit/
build/
dist/
.vite/
*.tsbuildinfo

# --- dbt / DuckDB ---
target/
dbt_packages/
logs/
*.duckdb
*.duckdb.wal
.user.yml

# --- Data: raw & large artifacts (regenerated by the pipeline) ---
data/raw/
*.parquet.tmp
vendor/
plotly*.min.js

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
*.log

# --- Personal scratch (never tracked) ---
_scratch/
CAJON/

# Sortides de l'eval de models (locals, mai committejades)
data/eval/
