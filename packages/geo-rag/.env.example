# Copy to .env (gitignored) and fill in — NEVER commit the real key.
# The `anthropic` SDK reads ANTHROPIC_API_KEY from the environment automatically.
# Only the OPTIONAL generative layer (extra [generativa]) uses it; the offline CI and
# the substrate/search paths do NOT.
ANTHROPIC_API_KEY=
